# netlify.toml
[build]
  command = ""
  publish = "."

[functions]
  directory = "netlify/functions/"

[[redirects]]
  # /api/fetch?url=... へのリクエストをプロキシ関数にリダイレクト
  from = "/api/fetch"
  to = "/.netlify/functions/proxy"
  status = 200
  # force = true # このルールが他のルールより優先されるようにする